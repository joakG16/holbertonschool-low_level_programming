#include "main.h"

/**
 *_abs -  a function that computes the absolute value of an integer.
 *@n: number to be checked
 *Return: 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
return (0);
}
